---
# cluster config
postgres_ha_cluster_master_host: "{{ play_hosts[0] }}"     # sync all DB slaves from this master node (set to first node if not defined)
postgres_ha_cluster_name: 'pgcluster'
postgres_ha_cluster_vip:  10.0.1.140
postgres_ha_cluster_vip_mask: 24
postgres_ha_pg_port:  5432
postgres_ha_cluster_vip_res_name:  'mgmt-vip'
postgres_ha_cluster_pg_res_name: 'postgres'         # slave DB cluster resource name
postgres_ha_cluster_pg_HA_res_name: 'postgres-ha'   # master DB cluster resource name (this name is used to manage postgres resource)

# auth config
postgres_ha_pg_repl_user: replicator
postgres_ha_pg_repl_pass: rybKath3KeckGov1
postgres_ha_cluster_ha_password: 'fropFav7epAbOch2'
# TODO compute hash automatically
postgres_ha_cluster_ha_password_hash: '$6$MHAki4YS$Nk7O3FEC2G.INznoSUj4ByFgdwFJ8mcI9.Ks3XAoLLe9f9GB36G8hZe9o8ygDySJwvnLVCn0LGPzcOapK42/A/'  # fropFav7epAbOch2

# postgres config
postgres_ha_pg_systemd_svcname: postgresql-9.5
postgres_ha_pg_data: /var/lib/pgsql/9.5/data
postgres_ha_pg_bindir: /usr/pgsql-9.5/bin

postgres_ha_postgresql_conf_vars:
    listen_addresses: "'*'"
    max_wal_senders: "{{ play_hosts|length }}"
    max_replication_slots: "{{ play_hosts|length }}"
    wal_level: "hot_standby"
    hot_standby: "on"
    wal_log_hints: "on"
